@model CompanyModel

@{
    ViewBag.Title = Html.LocalizationString("company_detail_view_title"); 
}

<div class="col-lg-12">
    <h3>@ViewBag.Title</h3>
    <hr />
     
        <div class="col-lg-12">
                <h2>
                    <span class="col-lg-4">@Model.Name</span>
                    <span class="col-lg-4">@Model.Address</span>
                </h2>
        </div> 
</div>